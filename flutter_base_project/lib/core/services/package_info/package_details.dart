class PackageDetails {
  final String appName;
  final String packageName;
  final String appVersion;
  final String buildNumber;

  PackageDetails(this.appName, this.packageName, this.appVersion, this.buildNumber);
}
